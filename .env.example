# Optional: Proof of Origin Token for YouTube API
# This helps prevent bot detection when fetching video metadata and transcripts
# Obtain this token from YouTube's internal API (advanced usage only)
# If not set, the application will still work but may experience issues on some servers
# PO_TOKEN=your_po_token_here

# Optional: Node environment (development/production)
# NODE_ENV=production
